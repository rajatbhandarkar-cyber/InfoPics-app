<% layout("/layouts/boilerplate") %>

<main class="flex-grow-1 d-flex align-items-center justify-content-center">
  <div class="row w-100" style="max-width: 680px;">
    <h3 class="text-center mb-3">Finish Creating Your InfoPics Account</h3>

    <% if (error && error.length > 0) { %>
      <div class="alert alert-danger text-center"><%= error %></div>
    <% } %>

    <% if (success && success.length > 0) { %>
      <div class="alert alert-success text-center"><%= success %></div>
    <% } %>

    <div class="col-12">
      <% const preview = tempUser || (req && req.session && req.session.tempUser) || {}; %>

      <div class="d-flex align-items-center mb-3">
        <div class="me-3">
          <img
            src="<%= preview.profilePic || '/images/default-avatar.png' %>"
            alt="avatar"
            width="72"
            height="72"
            class="rounded-circle"
          />
        </div>
        <div>
          <div class="fw-bold"><%= preview.email || '' %></div>
          <div class="text-muted">Signed in with Google. Your Google profile will be attached to this account.</div>
        </div>
      </div>

      <form action="/create-account" method="POST" class="needs-validation" novalidate autocomplete="off">
        <div class="mb-3">
          <label for="username" class="form-label">Choose a username</label>
          <input
            id="username"
            name="username"
            type="text"
            class="form-control"
            required
            minlength="3"
            maxlength="30"
            value="<%= preview.username || '' %>"
            placeholder="e.g., travelraj"
          />
          <div class="invalid-feedback">Please choose a username (3+ characters).</div>
        </div>

        <div class="mb-3">
          <label for="password" class="form-label">Create a password</label>
          <input
            id="password"
            name="password"
            type="password"
            class="form-control"
            required
            minlength="6"
            placeholder="At least 6 characters"
            autocomplete="new-password"
          />
          <div class="invalid-feedback">Please provide a password (6+ characters).</div>
        </div>

        <button type="submit" class="btn btn-success w-100">Create Account & Continue</button>
      </form>

      <div class="text-center mt-3">
        <a href="/signup" class="btn btn-link">Back to Sign Up</a>
        <a href="/logout" class="btn btn-link">Sign out and choose another Google account</a>
      </div>
    </div>
  </div>
</main>

<script>
  // Minimal bootstrap validation — does not replace server-side validation
  (function () {
    'use strict';
    const forms = document.querySelectorAll('.needs-validation');
    Array.prototype.slice.call(forms).forEach(function (form) {
      form.addEventListener('submit', function (event) {
        if (!form.checkValidity()) {
          event.preventDefault();
          event.stopPropagation();
        }
        form.classList.add('was-validated');
      }, false);
    });
  })();
</script>
